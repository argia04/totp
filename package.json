{
  "name": "totp-simple-auth",
  "private": true,
  "scripts": {
    "install-all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm run install-all && npm --prefix frontend run build",
    "start": "node backend/server.js"
  }
}
